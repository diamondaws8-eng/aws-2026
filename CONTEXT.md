# CONTEXT.md — مرجع المشروع للجلسات القادمة

> اقرأ هذا الملف أولاً عند بدء أي جلسة جديدة. آخر تحديث: **2026-09-08**.
>
> ⚠️ **هذا نظام يعمل في الإنتاج (Production) على بيانات حقيقية:** 368 طالباً، 19 معلماً، 18 فصلاً، ~304 حساب ولي أمر.
> **لا تحذف أي معلم أو طالب أو ولي أمر.** أي تغيير على قاعدة البيانات يجب التحقق منه قبل وبعد التنفيذ.

---

## 1. فكرة المشروع والتقنيات (Overview & Tech Stack)

### الفكرة
نظام إدارة مدرسية بالعربية (RTL) لـ **مدارس الأوس الأهلية**، بثلاث بوابات منفصلة:

| البوابة | المسار | الوظيفة |
|---|---|---|
| **المدير / الإدارة** | `/admin` | لوحة تحكم، الطلاب، المعلمون، المراحل، فريق الإدارة، الإعدادات، سجل التدقيق، نسخ احتياطية |
| **المعلم** | `/teacher` | تسجيل يومي للفصل (حضور، سلوك، واجب، أدوات، مشاركة)، نقاط، درجات، رسائل واتساب لأولياء الأمور |
| **ولي الأمر** | `/parent` | متابعة الابن: النقاط، الحضور، الملاحظات، الإشعارات |

الفكرة المحورية: المعلم يسجّل حالة كل طالب يومياً، فتتحوّل تلك الحالات إلى **نقاط** يراها ولي الأمر والمدير، مع لوحة صدارة ورسوم بيانية حيّة.

### التقنيات
- **Next.js 16.3.3** (App Router + Turbopack) — ⚠️ نسخة بها تغييرات جذرية عن المعتاد؛ راجع `node_modules/next/dist/docs/` قبل كتابة كود جديد (انظر `AGENTS.md`).
- **React Server Components + Server Actions** (`'use server'`)
- **Drizzle ORM 0.45.2** + `node-postgres` Pool
- **Supabase Postgres** — transaction pooler، المنفذ 6543، منطقة eu-west-1، **الخطة المجانية (500 ميجا)**
- **better-auth 1.7.2** — `hashPassword` من `better-auth/crypto`؛ الهاش يُخزَّن في جدول `account`
- **Tailwind v4** (`@theme inline`) + خطوط `next/font/google` (Cairo، Reem Kufi للزخرفة الإسلامية)
- رسوم بيانية **SVG مكتوبة يدوياً** (مؤشرات دائرية 270° عبر `describeArc`/`polarToCartesian`, ومنحنيات ناعمة بـ quadratic midpoints) — لا توجد مكتبة رسوم.
- React `cache()` لمنع تكرار الاستعلامات داخل الطلب الواحد.

### ملاحظات بيئية
- Windows، الشِل الأساسي PowerShell (وBash متاح)، مجلد العمل `F:\aws`
- ملف البيئة: **`.env.local`** (وليس `.env`)
- **لا يوجد `tsx` مثبّت محلياً**، و`npx tsx` يفشل في هذه البيئة. لتشغيل سكربت يحتاج `@/` alias: أنشئ مساراً مؤقتاً `app/api/devcheck/route.ts`، استدعِه بـ `curl`, ثم **احذفه**.
- `npx tsc --noEmit` و `npm run build` يعملان بشكل طبيعي.

---

## 2. بنية الملفات (Project Structure)

```
app/
  page.tsx                     شاشة اختيار البوابات
  admin/
    login/{page.tsx,login-form.tsx}   التسجيل الذاتي مسموح فقط قبل إنشاء المدرسة
    setup/                            التأسيس الأول (محجوب عن المعلم/ولي الأمر)
    (dashboard)/
      page.tsx                 الرئيسية — 14 استعلاماً متوازياً في Promise.all واحد (~640ms)
      hero-stats.tsx           بطاقات الإحصاءات العلوية
      daily-performance-cards.tsx  بطاقات الحضور/الغياب/الواجب/المشاركة (تظهر حسب مفاتيح الإعدادات)
      recent-notifications.tsx
      leaderboard-client.tsx
      students/ teachers/ grade-levels/ notifications/ settings/ staff/ audit/
  teacher/(dashboard)/
    actions.ts                 ⭐ أوامر المعلم (محمية الآن بـ requireTeacher*)
    classes/[classId]/page.tsx + class-roster.tsx
  parent/(dashboard)/
    actions.ts                 أوامر ولي الأمر
    set-password-card.tsx      إجبار تغيير كلمة المرور عند أول دخول
    subject/[subjectId]/page.tsx
  api/
    auth/[...all]/route.ts     better-auth
    daily-records/route.ts     ✅ صار محمياً (كان مفتوحاً للعالم)
    set-role/route.ts          ✅ صار مقصوراً على التأسيس الأول

lib/
  db/{index.ts,schema.ts}      Pool مُخزَّن على globalThis في التطوير، max: 15
  points.ts                    ⭐⭐ مصدر الحقيقة الوحيد للنقاط (جديد)
  admin-access.ts              ⭐ صلاحيات بوابة الإدارة + نطاق المراحل
  teacher-access.ts            ⭐ صلاحيات بوابة المعلم (جديد جداً)
  audit.ts                     سجل التدقيق
  utils.ts                     schoolDate() بتوقيت Asia/Riyadh

components/
  portal-layout.tsx            الشريط الجانبي المشترك + تسجيل الخروج بكتابة "aws"
  back-to-portals.tsx  brand-logo.tsx  theme-toggle.tsx
```

**ملفات لا تُرفع إلى git:** `deleted-duplicate-school-backup.json` (بيانات طلاب حقيقية) — **لا تستخدم `git add .`**.

---

## 3. ما تم إنجازه (Completed Features)

### أ) التصميم والواجهة
- تصميم "خرافي" لكل صفحات بوابة المدير: تدرجات، رسوم SVG يدوية، مؤشرات دائرية.
- بطاقات الأداء اليومي (حضور/غياب/واجب/مشاركة) تظهر أو تختفي حسب المفاتيح في الإعدادات، بنِسب حيّة يوماً بيوم.
- التنقل للخلف حتى **7 أيام** في الرئيسية.
- اسم المدرسة مرة واحدة بخط `Reem Kufi` أعلى الشريط الجانبي، ومسمّى الدور تحت اسم المستخدم.
- زر "العودة لشاشة البوابات" في شاشات الدخول الثلاث.

### ب) الصلاحيات والأدوار
`lib/admin-access.ts` — أربعة أدوار:
| الدور | المفتاح | الوصف |
|---|---|---|
| المالك والمدير التنفيذي | `owner` | كل الصلاحيات، لا يمكن حذفه |
| مدير الجودة | `quality_manager` | كل شيء عدا حذف المالك، ونسخة احتياطية كاملة |
| مدير مدرسة | `principal` | مراحل محددة أو الكل + نسخة لمراحله |
| وكيل | `deputy` | مرحلة محددة، مع خيار قراءة فقط |
كلهم يدخلون من بوابة الإدارة ولكلٍّ صفحة إعدادات لتغيير اسمه/بريده/كلمة مروره.

### ج) الأمان (تم إصلاحه هذه الجلسة)
- ✅ حُذفت **19 كلمة مرور معلم مخزّنة كنص صريح** (وتم التحقق أن الـ19 لا يزالون يدخلون).
- ✅ 282→368 ولي أمر: إجبار تغيير كلمة المرور عند أول دخول (`user.mustChangePassword`) دون إغلاق أي حساب. الافتراضية للجديد `12345678` ولم تعد معروضة علناً.
- ✅ أُغلق التسجيل الذاتي في بوابة الإدارة، وحُجب `/admin/setup` عن المعلم وولي الأمر.
- ✅ **حُذف `/api/test-add`** — مسار تجريبي مفتوح يسرّب stack traces.
- ✅ **`/api/set-role`** كان يسمح لأي حساب أن يجعل نفسه `admin` → قُصر على حالة عدم وجود مدرسة.
- ✅ **`/api/daily-records`** كان **بلا أي مصادقة** ويسرّب حضور وسلوك وملاحظات كل الطلاب → صار يتطلب حساب معلم + ملكية الفصل.
- ✅ **كل أوامر بوابة المعلم** كانت تكتفي بـ"هل توجد جلسة؟" — أي ولي أمر كان يستطيع منح ابنه نقاطاً أو تعديل الحضور. أُنشئ `lib/teacher-access.ts` (`requireTeacher` / `requireTeacherForClass`) وطُبِّق على الأوامر الثمانية، مع أخذ `schoolId` من السجل لا من المتصفح، والتحقق أن الطالب في الفصل فعلاً، وحدّ ±100 للنقاط اليدوية.

### د) نظام النقاط — **مُشتق لا مُكرَّر** ⭐ (أهم تغيير معماري)
كان النظام يكتب **5 صفوف** في `student_points` لكل طالب كل يوم، رغم أن `daily_records` يخزّن أصلاً كل الحالات **و** `pointsEarned`. أي أن 81% من نمو قاعدة البيانات كان تكراراً خالصاً.

الآن في [lib/points.ts](lib/points.ts):
- `derivePointEntries(rec, settings)` — مصدر الحقيقة الوحيد لتحويل الحالة إلى نقاط، يستخدمه الحفظ والعرض معاً.
- `totalPointsFor` / `getStudentPointsTotal` / `getClassPointsTotals` / `getStudentTeacherPointsTotal` / `getLeaderboard` / `getManualPoints` / `getStudentPointsHistory`
- المجاميع = `SUM(daily_records.points_earned)` + `SUM(student_points.points)`
- `student_points` لم يعد يحتوي إلا **النقاط اليدوية** (`type='manual'`) لأنها غير قابلة للاشتقاق.
- `resyncSchoolPoints(schoolId, settings)` — يعمل تلقائياً عند حفظ الإعدادات: أمر SQL واحد يصحّح كل السجلات، و`IS DISTINCT FROM` يتخطّى ما لم يتغيّر. **ملاحظة تصميمية:** هذا يعيد حساب **الأيام الماضية** بالقيم الجديدة (اتساق مقابل تجميد التاريخ).

**الأثر على المساحة** (قياس فعلي: 328 بايت للصف في `daily_records`، 319 في `student_points`، 180 يوماً دراسياً):

| | 368 طالب (الآن) | ~700 طالب (مع الثانوية) |
|---|---|---|
| قبل | ~124 ميجا/سنة → 4 سنوات | ~245 ميجا/سنة → سنتان |
| **بعد** | **~27 ميجا/سنة → 18 سنة** | **~52 ميجا/سنة → 9 سنوات** |

الحجم الحالي: **12 ميجا** من 500.

### هـ) الأداء
- الرئيسية: من **3–4.6 ثانية → ~640ms** (14 استعلاماً في `Promise.all` واحد + `cache()` + Pool ثابت).
- حفظ الفصل كله صار **أمر SQL واحد** (`ON CONFLICT`) بدل حلقة تكتب صفاً صفاً.

### و) وظائف أخرى
- نسخ احتياطية: تحميل JSON + **استرجاع** من نفس الملف، بصلاحيات حسب الدور.
- سجل تدقيق (`audit_log`) لكل عملية حساسة.
- كلمات مرور مؤقتة يصدرها المدير للمعلم/ولي الأمر عند النسيان (لا يفقد صاحب الحساب أي بيانات).
- تسجيل الخروج يتطلب كتابة **"aws"** (بأي حالة أحرف)، والضغط على الشعار **لا** يسجّل الخروج.
- تعديل اسم المرحلة، وتعديل المدير لاسمه.
- توقيت المدرسة موحّد على `Asia/Riyadh` عبر `schoolDate()`.

---

## 4. حالة قاعدة البيانات والـ APIs

### الجداول (18)
**better-auth (لا تُعاد تسميتها):** `user` (+ `mustChangePassword`)، `session`، `account`، `verification`

**النظام:**
`schools` · `school_staff` · `audit_log` · `grade_levels` · `classes` · `teachers` · `students` · `subjects` · `attendance` · `grade_entries` · `notifications` · `daily_records` · `student_points` · `parent_whatsapp_messages`

### فهارس مهمة على `daily_records` (الجدول الأسرع نمواً)
- `daily_records_school_date_idx (school_id, date)`
- `daily_records_class_date_idx (class_id, date)`
- `daily_records_student_idx (student_id)`
- ⭐ `daily_records_student_class_date_uq` — **فريد** على `(student_id, class_id, date)`؛ أُنشئ على القاعدة الحيّة ويمنع تكرار صف اليوم الواحد (تم اختباره بحفظين متزامنين → 3 صفوف لا 6).

### أعمدة مهملة
- `teachers.tempPassword` — `@deprecated`، لم يعد يُكتب فيه.
- `student_points.dailyRecordId` — لم يعد له معنى بعد تحويل النقاط للاشتقاق.

### حالة البيانات الفعلية (تحقق منها قبل أي افتراض)
| الجدول | العدد |
|---|---|
| students | 368 |
| teachers | 19 |
| classes | 18 |
| **subjects** | **0** ⚠️ |
| daily_records | 0 |
| student_points | 0 |

### المسارات (API Routes)
| المسار | الحالة |
|---|---|
| `GET/POST /api/auth/[...all]` | better-auth |
| `GET /api/daily-records?classId&date` | ✅ محمي: جلسة معلم + ملكية الفصل |
| `POST /api/set-role` | ✅ محمي: التأسيس الأول فقط |

معظم المنطق في **Server Actions** لا في REST: `app/*/(dashboard)/actions*.ts`.

---

## 5. المهام القادمة (Next Steps / TODO)

مرتّبة حسب الأولوية. البنود 🔴 تعطّل وظائف قائمة فعلاً.

### 🔴 1. لا توجد أي مادة دراسية في قاعدة البيانات (`subjects = 0`)
`subjects` هو **الرابط الوحيد** بين المعلم والفصل، وهو فارغ. النتائج:
- قسم "المواد" في بوابة ولي الأمر فارغ تماماً.
- `getSubjectDetails` و`saveGrades` و`gradeEntries` (نظام الدرجات كله) **معطّل**.
- لا يمكن تقييد المعلم بفصوله لأنه لا توجد بيانات تربطه بها.
**المطلوب:** واجهة لإضافة المواد لكل فصل وإسناد معلم لكل مادة (أو قرار بنموذج ربط مختلف).

### 🔴 2. كل معلم يرى كل فصول المدرسة
`app/teacher/(dashboard)/classes/page.tsx` يجلب **كل** الفصول بلا أي تصفية. أي معلم يستطيع فتح أي فصل وتسجيل الحضور والنقاط وإرسال رسائل لأي ولي أمر. مرتبط بالبند 1 (يحتاج جدول ربط أولاً).

### 🟠 3. سجل يومي واحد لكل فصل
القيد الفريد `(student, class, date)` يعني أنه لو سجّل معلمان لنفس الفصل في نفس اليوم، الثاني **يمسح** عمل الأول. **قرار مطلوب:** هل يصبح المفتاح `(student, class, date, teacher)`؟ (سيؤدي لجمع النقاط عبر المعلمين) أم يبقى سجلاً موحّداً للفصل؟

### 🟠 4. استرجاع نسخة احتياطية قديمة سيضاعف النقاط
في `app/admin/(dashboard)/settings/actions-settings.ts` (~سطر 345) يُدرج `d.studentPoints` كاملاً. أي نسخة أُخذت قبل تحويل النقاط للاشتقاق تحتوي صفوف `attendance/behavior/...` التي ستُجمع فوق `daily_records.pointsEarned`.
**الإصلاح:** `d.studentPoints.filter(r => r.type === 'manual')` عند الاسترجاع.

### 🟡 5. اختبار نظام الأدوار بدخول فعلي
لم يُختبر أبداً بحساب "مدير جودة" أو "وكيل" حقيقي. **يحتاج المستخدم** — المساعد لا يُدخل كلمات مرور.

### 🟡 6. مراجعات أمنية لم تُنفَّذ بعد
- صلاحيات **أوامر بوابة الإدارة** (لم تُراجع بنفس دقة بوابة المعلم).
- بقية **أوامر بوابة ولي الأمر** عدا النقاط.
- **الإشعارات** و**استيراد الطلاب من إكسل**.
- **لا توجد حماية من تكرار محاولات الدخول** (rate limiting) في أي بوابة.

### 🟡 7. قرارات تصميمية معلّقة
- هل تُجمَّد نقاط الأيام الماضية عند تغيير الإعدادات، أم تبقى تُعاد الحسبة (السلوك الحالي)؟
- عند اقتراب امتلاء الخطة المجانية بعد سنوات: **أرشفة** السنوات الدراسية المنتهية في ملف بدل حذفها.
- حذف العمودين المهملين (`teachers.tempPassword`، `student_points.dailyRecordId`).

---

## قواعد عمل مهمة في هذا المشروع

1. **الإنتاج مباشر** — تحقق من عدد الصفوف قبل وبعد أي عملية على البيانات.
2. **`tsc` لا يكفي.** خطأ `export const` داخل ملف `'use server'` أسقط صفحتين حيّتين ولم يلتقطه `tsc` — شغّل `npm run build` وافتح الصفحة فعلياً.
3. **الصراحة الكاملة مطلوبة** — المستخدم طلب صراحة: "ولازم متتجملش، أنا عاوز الحقيقة". اذكر ما لم تفحصه، لا تدّعِ أن كل شيء سليم.
4. المستخدم يتواصل بالعربية؛ ردّ بالعربية.
5. Next.js في هذا المشروع **ليس** النسخة المعتادة — راجع `AGENTS.md` و`node_modules/next/dist/docs/`.
