# HANDOFF — سجل المحادثة الكامل (٨ – ٩ سبتمبر ٢٠٢٦)

> **لمن يقرأ هذا:** هذا ملف تسليم (handoff) لجلسة محادثة كاملة امتدت من ٢٠٢٦-٠٩-٠٨ الساعة ٠٢:٤٥ حتى ٢٠٢٦-٠٩-٠٩ الساعة ٠٥:٢٥.
> اقرأه بالكامل قبل أن تكتب سطراً واحداً. ثم اقرأ `CONTEXT.md` للمرجع التقني العام للمشروع.
>
> **الترتيب الصحيح للقراءة:** `AGENTS.md` ← `CONTEXT.md` ← هذا الملف.
>
> رسائل المستخدم في القسم ٣ منقولة **بالحرف** من سجل المحادثة، بأخطائها الإملائية كما كُتبت — لأن نبرتها جزء من المعنى.

---

## ٠. الحالة عند كتابة هذا الملف

| | |
|---|---|
| الفرع | `main` — متزامن مع `origin/main` |
| آخر commit | `70e56d5` — تشغيل نظام الإشعارات فعلياً |
| `npx tsc --noEmit` | نظيف |
| `npm run build` | ناجح |
| ملفات مؤقتة متبقية | لا شيء |
| العمل الجاري | لا شيء — كل ما طُلب نُفِّذ ورُفع |

**التحذير الأهم:** هذا نظام يعمل في الإنتاج على بيانات حقيقية — ٣٦٨ طالباً، ١٩ معلماً، ١٤ فصلاً، ٢٨١ حساب ولي أمر، ٤ من فريق الإدارة. لا تحذف أحداً. أي تعديل على قاعدة البيانات يُتحقَّق منه قبل وبعد.

---

## ١. من هو المستخدم وكيف يريد أن يُعامَل

**د/ حسام المويس** — مالك النظام ومديره. مدارس الأوس الأهلية. البريد: `qd@alaws.edu.sa`.

قواعد تعامل استخلصتها من المحادثة — **التزم بها**:

1. **لا تتجمّل.** قالها صراحةً: *«لازم متتجملش، أنا عاوز الحقيقة»*. حين تفشل، قل فشلت. حين لا تتحقق من شيء، قل لم أتحقق.
2. **لا شيء وهمي.** *«واياك يكون هناك اي شي وهمي»* — لا أرقام مُقدَّرة تُعرض كأنها محسوبة، ولا نِسب لا تجمع، ولا رسائل نجاح عن عمل لم يتم.
3. **تحقّق قبل أن تحكم.** *«عدم التسرع بالحكم علي شي قبل ان تتاكد لان من الممكن ان تعتبر شي خطا وهو بالاصل يعمل وله وظيفه مهمه»*. هناك قسم كامل في هذا الملف (القسم ٦) لأشياء بدت أخطاءً وليست كذلك.
4. **يفضّل العمل على دفعات:** «حل رقم ١ و رقم ٢»، «حل الاثنين للي عليهم الدور»، «اكمل». اعرض قائمة مرقّمة ودعه يختار.
5. **يسأل رأيك قبل التنفيذ أحياناً**، وحين يفعل يقول صراحةً *«لا تقم بالتنفيذ ولكن قول لي»* — فلا تنفّذ.
6. **يريد الجودة العالية:** *«ابدع في هذا الشي ولا تنس اي شي»*، *«وهيا ابهرني»*.
7. لغته العربية العامية المصرية/السعودية، ويكتب بسرعة وبأخطاء إملائية. الرد بالعربية الفصحى المبسّطة.

---

## ٢. حقائق ثابتة عن النظام

- **المسار:** `F:\aws` — Windows، PowerShell أساسي وBash متاح.
- **النشر:** Vercel → `aws-2026.vercel.app`. المستودع: `github.com/diamondaws8-eng/aws-2026`.
- **البيئة:** `.env.local` (وليس `.env`).
- **Next.js 16.3.3** App Router + Turbopack — ⚠️ نسخة بها تغييرات جذرية؛ اقرأ `node_modules/next/dist/docs/` قبل كتابة كود جديد.
- **Drizzle ORM 0.45.2** + `node-postgres` Pool → **Supabase** transaction pooler، منفذ 6543، eu-west-1، **الخطة المجانية (٥٠٠ ميجا)**.
- **الهجرات:** لا يوجد مجلد `drizzle/` — النشر عبر `npm run db:push` (drizzle-kit push، مقارنة مباشرة بالمخطط).
- **better-auth 1.7.2** — جلسة واحدة مشتركة بين البوابات الأربع؛ `auth.api.getSession({ headers })`.
- **لا يوجد `middleware.ts`.**
- **البوابات الأربع:** `/admin`، `/teacher`، `/counselor`، `/parent`.

### كيف تستعلم قاعدة البيانات الحيّة
`npx tsx` يفشل في هذه البيئة، والسكربت خارج مجلد المشروع لا يجد `node_modules`. الطريقة التي نجحت:

```bash
# أنشئ الملف داخل جذر المشروع (كي يجد pg)، شغّله، ثم احذفه
cat > F:/aws/tmp-dbcheck.mjs <<'EOF'
import fs from 'fs'
import pg from 'pg'
const url = fs.readFileSync('.env.local','utf8').match(/^DATABASE_URL="?([^"\n]+)"?/m)[1]
const pool = new pg.Pool({ connectionString: url })
console.table((await pool.query(`select 1`)).rows)
await pool.end()
EOF
node F:/aws/tmp-dbcheck.mjs; rm -f F:/aws/tmp-dbcheck.mjs
```

⚠️ **احذف الملف دائماً.** في هذه الجلسة نسيت حذف `app/api/devcheck/route.ts` مرة لأن فشل `curl` قطع سلسلة `&&`. استخدم `;` لا `&&` قبل الحذف.

⚠️ **مخرجات العربية في الطرفية تظهر كرموز مشوّهة (mojibake)** — هذا خلل عرض في الطرفية فقط، والبيانات سليمة. للتحقق: `python -c "import sys; sys.stdout.reconfigure(encoding='utf-8'); ..."`.

⚠️ **ملفات المشروع تستخدم نهايات أسطر CRLF.** استبدال النصوص عبر Python يفشل أحياناً بصمت؛ استخدم أداة `Edit` عند الشك.

---

## ٣. سرد المحادثة بالترتيب — كل رسالة بالحرف وما تلاها

### (١) البداية

> **المستخدم:** `@"C:\Users\CENTER_ELRahama\Downloads\CONTEXT.md"`
> اقرأ ملف @CONTEXT.md لتفهم حالة المشروع الحالية، ثم استكمل معي العمل عندما اطلب منك اي شي فقط قل لي جاهز

### (٢) تدقيق بوابة المعلم

> **المستخدم:** قل لي هل بوابة المعلم تنقصها شي ؟

قدّمت قائمة مرقّمة بما ينقص بوابة المعلم.

> **المستخدم:** حسنا حل رقم 1 و رقم 2
> **المستخدم:** طيب حل الاثنين للي عليهم الدور
> **المستخدم:** ما هو امر الرفع ل جيت هوب من ال vs code
> **المستخدم:** طيب حل الاتنين اللي بعدهم
> **المستخدم:** اعمل اللي بعده
> **المستخدم:** طيبب اي اللي باقي من اللي قولنا عليه

نتج عنها commit `91933fe` — «إصلاح استرجاع النسخ الاحتياطية وسبع مشكلات في شاشة المعلم».

### (٣) كارثة النِسب — أهم نقطة تحوّل

> **المستخدم:** *(مع صورة)* شوف المصيبه دي النسب ازاي كدا بايظه خالص يعني المفروض ازاي نسبه الغياب مع الحضور تساوي 102 من 100 ازاي عاوزك تركز واياك تخطأ في اي نسبه او اي شي **واياك يكون هناك اي شي وهمي**

تحققت من قاعدة البيانات الحيّة: ٣٣٩ حاضر / ١٦ غائب / ٧ متأخر / ٦ بعذر = ٣٦٨. كل رقم صحيح منفرداً، لكن ١٣ حالة (متأخر + بعذر) كانت غير مرئية في العرض.

> **المستخدم:** *(مع صورة)* ما وال هناك الخطا المفروض المجموع داييما يكون 100 ازاي كدا 96 المفروض يبقي المجموع مناسب مع بعضه حتي يكون 100 احسب صح نسبه الغياب والحضور **وبالنسبه للطالب الغايب اما يحدد غايب من اي معلم لا يمكن ان يقوم اي معلم اخر بتحضيره** ولكن عندما يحاول اي معلم ان يقوم بعمل تحضيره او حتي غيابه او تاخير يظهر له كارت ان المعلم فلان قام بتسجيل غياب هذا الطالب كما انه يسجل غايب في باقي الحصص بالكامل عند المعلمين عندما يقومون بعمل حفظ ولا تخطأ في الحساب **وهيا ابهرني**

> **المستخدم:** عموما العيب في اما المعلم بيختار ان في طالب اذن او متأخر هو دا سبب خراب المشكله

**الحل:** أُعيد تعريف النِسب — الحضور = حاضر + متأخر، الغياب = غائب + بعذر — مع تقريب **largest-remainder** (`wholePercents` في `lib/utils.ts`) يضمن أن المجموع ١٠٠ بالضبط.

**كذلك:** الرسم البياني للاتجاه كان يرسم أيام «لا بيانات» كصفر ٪. الحقل `hasAttendance` كان موجوداً لكن `TrendChart` لم يستعمله قط. أُصلح بفواصل (gap segments).

### (٤) إعادة هيكلة: حضور مشترك، تقييم لكل معلم

> **المستخدم:** عندي استفسار بس معلش المفروض ان كل معلم ملهوش علاقه باللي المعلم التاني عامله وهو اه كلها نسبه واحده تتحسب حسب المعلمين اللي سجلوا للفصل اليوم بمعني ان الحضور والغياب يبقي موحد عند الكل من اول معلم ذي ما معمول بس بالنسبه للباقي ف كل معلم له الحرية انه يشخص الطالب وكل النسب دي تتحسب كنسبه واحده وايضا كل كارت اجعله تفصيلي بمعني لو طالب احضر ادواته في ماده ولم يحضرها في ماده اخري وهذا المعلم قام بتسجيل انه جاء بالادوات والتاني لا ف يمكنك حسابها بطريقتك انت **وابدع في هذا الشي ولا تنس اي شي** لان هذا هو اقوي اقوي تفصيله نريدها وكما قلت الغياب والحضور اجعله من المعلم الاول الذي يقوم بتسجيل الفصل مع امكانيه فتح الاذن للطالب ان حب طالب يستاذن لانه وارد ولكن **لا يمكن فتح وتغير طالب غايب الا من المعلم الذي سجله غياب** واما عن كل شي باقي مثلا السلوك والادوات وغيره ف اختيار المعلم يبقي له وحده ولا يفرض علي الباقي واعطني رايك

**ما نُفِّذ (commit `484d51d`):** فُصل جدول واحد إلى جدولين:

| الجدول | المفتاح الفريد | ماذا يحمل |
|---|---|---|
| `daily_records` | (طالب، فصل، يوم) | **الحضور فقط** — مشترك بين كل معلمي الفصل + قفل الغياب |
| `lesson_records` | (طالب، معلم، يوم) | **تقييم كل معلم** — سلوك، واجب، أدوات، مشاركة، ملاحظة |

**قفل الغياب** (`app/teacher/(dashboard)/actions.ts`):
```ts
const OUT_OF_SCHOOL = ['absent', 'excused'] as const
if (standingAbsence && prev!.absenceMarkedBy !== userId) {
  if (attendanceStatus !== prev!.attendanceStatus) blockedIds.push(rec.studentId)
  attendanceStatus = prev!.attendanceStatus as typeof attendanceStatus
  absenceMarkedBy = prev!.absenceMarkedBy; absenceMarkedAt = prev!.absenceMarkedAt
} else if (isOutOfSchool(attendanceStatus)) {
  absenceMarkedBy = standingAbsence ? prev!.absenceMarkedBy : userId
  absenceMarkedAt = standingAbsence ? prev!.absenceMarkedAt : new Date()
}
```
أول معلم يسجّل الغياب يملكه؛ لا معلم آخر يستطيع تحضيره؛ والغياب يسري على كل الحصص التالية؛ ويظهر لمن حاول كارت باسم المعلم صاحب التسجيل.

**تحليل التخزين** الذي عُرض على المستخدم ووافق: ~٢١ ميجا/سنة ← ~١٢٠ ميجا/سنة؛ الخطة المجانية تكفي ~٢٣ سنة ← ~٤ سنوات. ووافق أيضاً على تضخّم النقاط (~٧ ← ~٣٥ يومياً).

### (٥) خطأي: ضياع ٦ نقاط لكل طالب

> **المستخدم:** *(مع صورة)* حاليا انا الطلاب دول كان المفروض كانوا الاعلي وكل طالب كان 7 نقاط والان عندما قمت باعطائهم او تسجيل من خلال معلم اخر والمفروض النقاظ تجمع علي ي بعضها ولك ما حدث انني تفاجئت ان كل الطلاب اصبحوا نقطه 1 اي المشكله ؟؟؟؟؟

> **المستخدم:** *(مع صورة)* طيب ان تحاسب ان الطالب واخد نقطه واحده ليه مع العلم ان المعلم بيعطي للطالب نقاط اصلا اعلي من كدا في المره او الحصه بتةصل لسبعه غالبا لو كان حاضر وملتوم وواجبه وكله احب صح **وبلاش تتغابي**

**كان خطئي أنا.** الهجرة نسخت التقييمات إلى `lesson_records` بـ `points_earned = 0`، وافترضت أن المجموع القديم في `daily_records` يغطّيها. أول حفظ من أي معلم أعاد كتابة الحضور ليصير حضوراً فقط — فاختفت الـ٦ نقاط. أُصلح بإعادة الحساب في الجدولين عبر SQL (٣٥٩ صف `lesson`، ٢٨١ صف `daily`).

**وخطأ ثانٍ أخطر:** عدّلت بيانات الإنتاج بينما `git log` كان يُظهر أن الموقع المنشور ما زال يعمل بالـ commit القديم. وكنت قد قلت للمستخدم *«لا يحتاج رفع كود»* — وكان ذلك خطأً. لذلك ظهرت لوحة الصدارة برقم ١ بدل ١٢.

> **القاعدة المستخلَصة:** **انشر الكود قبل أن تهاجر البيانات. تحقق من `git log` قبل أن تدّعي أن الكود منشور.**

> **المستخدم:** معلش فين ارفعه ما ترفعه انت
> **المستخدم:** طيب هل يبقي شي لم نفعله مما قولت عنه ؟

### (٦) مراجعة أمنية شاملة

> **المستخدم:** تمام ابدا / كمل / مش فاهم ايوا اي اللي انت عملته انا مش فاهم حاجه / طيب اي اللي عليه الدور / ايوا ابدا بالاولي

commits: `02de029` (سد خمس ثغرات صلاحيات في بوابتَي الإدارة وولي الأمر)، `c91c1a3` (إغلاق تسريب سجل الفصل وإجبار المعلم على تغيير كلمة مروره).

**خطأ ثالث لي، أمني:** أثناء فصل الجدولين استخرجت `getRosterForDay` وصدّرتها من ملف `'use server'` — وكل تصدير من ملف `'use server'` هو **نقطة نهاية HTTP عامة**. صار سجل فصل كامل قابلاً للقراءة بلا جلسة. نُقلت إلى `lib/daily-roster.ts`.

> **هذه هي الفكرة الأمنية المتكررة في المشروع كله: كل `export` من ملف `'use server'` هو endpoint عام. لا تصدّر منه إلا ما تحرسه.**

### (٧) عطل حقيقي في الإنتاج: أولياء الأمور لا يستطيعون الدخول

commit `54a6d02`. ٢٨٠ من ٢٨٣ حساب مخزّن بصيغة `5XXXXXXXX`، بينما شاشة الدخول كانت تبني البريد من الأرقام كما تُكتب. صيغة واحدة من خمس كانت تنجح، والرسالة كانت تقول «كلمة المرور خاطئة» — وهي ليست كذلك. أُصلح بـ `parentEmailCandidates` في `lib/utils.ts` التي تجرّب كل الصيغ.

### (٨) «حل جميع المشاكل»

> **المستخدم:** الان حل جميع المشاكل ملا اريد ولو مشكله واحده افعل ذلك حتي لدرجه عندما اسالك مره اخري تقول ان ليس هناك مشاكل او اي خطر واننا جاهزين للعمل بدون اي قلق ولا خوف

### (٩) مراجعة بوابة المدير قسماً قسماً

> **المستخدم:** تمام حلو بالنسبه لجميع اقسام بوابة المدير عاوزك تراحعهم قسم قسم واريدد منك **عدم التسرع بالحكم علي شي قبل ان تتاكد** لان من الممكن ان تعتبر شي خطا وهو بالاصل يعمل وله وظيفه مهمه يجب التاكد من كل شي

> **المستخدم:** اكمل

commit `ec59961` — سبع مشكلات. ونتج عنها أيضاً قائمة «ليست أخطاءً» في القسم ٦ أدناه.

### (١٠) فكرة الموجه الطلابي

> **المستخدم:** تمام الان لنبدا في شي مهم جدا جدا جدا **لا تقم بالتنفيذ** ولكن قول لي ان رايي صحيح او لا بدون تاخير وبدون ان تجانل وبدون ان تلمس شي وبدون ان تنفذ شي فقط جاوب ما رايك ان نقوم بجعل الرساله السلبية التي تذهب الي ولي الامر يتم بدالها ان ترسل للموجه الطلابي التي سوف نقوم باضافته اما ببوابه خاصه به او في بوابه الاداره حسب ما تقول انت ايهما افضل والموجه هو الذي اما يقوم بارسال الرساله لولي الامر او ان يقوم بحل المشكله هو بينه وبين الطالب بدون ان يعلم ولي الامر حفاظا علي المشاكل ولا تقم بتنفيذ شي فقط جاوب ول لي ما الذي سيكون من صلاحيه ومزايا الموجه الطلابي

> **المستخدم:** طيب جميل رايك بس اي رايك اما نشارك الوكيل بتاع المرحله او المراحل في الموضوع كمان ولا انت شايف اي ؟؟؟

> **المستخدم:** خطتك رائعه

> **المستخدم:** **لا تجعل حالات التي تتجاوز الموجه** وتصل لولي الأمر دائما كل شي يتم توجيهه للموجه **ولا تني ان من الممكن ان يكون هناك اكثر من وكيل للمرحله او المراحل ف عليك الانتباه ان الموجه عندما يفكر في التصعيد يجب عليه ان يحدد من الوكيل** وتوكل علي الله

**قراران ملزمان:**
1. **لا استثناء يتجاوز الموجه.** لا «حالة طارئة» تذهب لولي الأمر مباشرة.
2. **التصعيد يجب أن يسمّي وكيلاً بعينه** — لأن للمرحلة الواحدة قد يكون أكثر من وكيل.

commit `cf9bf4b` — «الموجه الطلابي: لا تصل ملاحظة سلبية إلى بيت قبل أن يقرأها».

### (١١) الوكيل يتصرّف لا يقرأ فقط

> **المستخدم:** *(مع صورة)* طيب حاليا ان صعدت الطلب للوكيل ولكنها تزهر للوكيل كبيانات **نحن نريد ان يقوم الوكيل ايضا بالتصرف بالامر** وكما اننا نريد عندما يقوم المعلم بارسال الطالب للموجه المفرووض توصل بجميع بياناته مشروحه بالتفصيل ولا اي رايك بما فيهم كتابة ولي الامر ولكن قول رايك ان لم يكن مهم

commit `677e42d` — «الوكيل يتصرّف في الحالة المحالة، والموجه يرى ما جُرِّب من قبل». أُضيف `app/admin/(dashboard)/cases/actions-cases.ts` وعمود `admin_note`.

### (١٢) واتساب لا يفتح

> **المستخدم:** عندما احدد ابلغ ولي الامر واقوم بالضغط علي اتخاذ قرار من المفروض ان يحولني علي رقم ولي الامر علي واتساب ولكن لا يحدث ذلك لا من خلال الموجه او من الوكيل ولا يحدث اي شي من ذلك ف انظر ما الحل

**السبب:** `window.open` بعد `await` يحجبه المتصفح كنافذة منبثقة. **الحل (commit `fbfe308`):** الخادم يعيد `waUrl`، والتبويب يُفتح **بشكل متزامن داخل نقرة المستخدم**، ثم يتم التحويل بعد الحفظ.

> **قاعدة:** أي فتح تبويب يجب أن يحدث داخل إيماءة النقر نفسها، لا بعد `await`.

### (١٣) نظام الإشعارات

> **المستخدم:** تمام جربتها بجد تحفة بس في حاجه لازم نسال عنها عندما يتم الارسال لولي الامر او اخباره لماذا لا يتم اشعاره في بوابته **انا اريد ان تضيف نظام الاشعارات بالكامل لدي الاربع بوابات وان تضيف بالاعلي جرس اشعارات لكل من يجب ان يذهب له اشعارات ولا تنسي اي شي ولكن ادرس الموضوع بدقة حتي لا نقع في اي مشكله او خطا**

commit `6a23a3c` — «نظام إشعارات موحّد وجرس في البوابات الأربع». جدول `user_notifications` + `lib/notifications.ts` + `components/notification-bell.tsx` + صفحة إشعارات لكل بوابة.

### (١٤) مكان الجرس — ثلاث جولات

> **المستخدم:** *(مع صورة)* لا تجعل الجرس في القايمه الجانبية بل اجعله في الصفحه بالاعلي وايضا عندما اضغط عليه يحولني علي صفحه اشعاراتي ما ررايك

commit `a189c52` — شريط علوي.

> **المستخدم:** *(مع صورة)* طان بامانك ان تجعله هنا *(بسهم يشير بجوار عنوان الفصل)*

سألته بين خيارين، فأجاب:

> **المستخدم:** **الغ الشريط من كل الصفح والبوابات واجعله ف محتوي الصفحه**

commit `5028448` — أُلغي الشريط نهائياً؛ الجرس صار داخل رأس كل صفحة، ويعرف بوابته بنفسه من `usePathname()`. أُدرج في ١٧ صفحة + `class-roster.tsx` + `components/notifications-page.tsx`. وحُذف `components/page-header.tsx` غير المستخدم. وعادت شاشة الفصل إلى `h-screen`.

**مشكلة الدفع:** علِق `git push` مراراً، وأُنهيت ست عمليات `git` عالقة. نجح لاحقاً عبر مهمة خلفية.

### (١٥) «نظام الإشعارات لا يعمل» — آخر طلب

> **المستخدم:** تمام نظام قسم الاشعارات نفسه لا يعمل بمعني لا يصل اي اشعار لاحد ف قم بدراسه الموضوع بالكامل  وجميع الاشعارات المستخدمه لكي يعمل كل شي بسلاسه عند الجميع

**التشخيص — النظام لم يكن معطلاً:**

استعلمت قاعدة البيانات الحيّة فوجدت:

| ما فُحص | النتيجة |
|---|---|
| `user_notifications` | **٢٨١ صفاً** — كُتبت فعلاً |
| مطابقة المستلمين لحسابات حقيقية | ٢٨١ من ٢٨١، **صفر يتيم** |
| المقروء | **صفر** |

**ثلاثة أسباب، لا عطل واحد:**

1. **الأكبر:** كل الـ٢٨١ ذهبت لأولياء الأمور، و**٢٧٩ منهم لا يستطيعون فتح البوابة أصلاً**. `app/parent/(dashboard)/layout.tsx` يستبدل البوابة كاملةً بشاشة «اختر كلمة مرور» حين `mustChangePassword` — فلا قائمة ولا صفحة ولا **جرس**. من ٢٨١ حساب ولي أمر، **حسابان فقط مفعّلان**.
2. الحالات السلوكية الست وقعت ٢١:٢٨–٢٢:٠٥، وكود الإشعارات رُفع **٢٢:٢٠**. كلها سبقت الكود — فلم يُكتب لها إشعار واحد. الميزة لم تُجرَّب مرة واحدة منذ رفعها.
3. لم يُوجَّه للمدير أي إشعار قط — «التنبيهات» كانت تذهب لأولياء الأمور فقط. فحين نظر في جرسه، «لا توجد إشعارات» كان جواباً صحيحاً حرفياً.

**ما أُصلح (commit `70e56d5`):**
- **وجهة للتنبيه:** أولياء الأمور / المعلمون والإدارة / الجميع — وكل مستلم يُنقل لصفحة إشعاراته في بوابته هو (`staffForAnnouncement` تعيد `href` لكل شخص).
- **إشعار غياب لولي الأمر** عند أول تسجيل غياب في اليوم، مرة واحدة، لليوم الحالي فقط، والغياب بعذر لا يُرسَل. حراسة مزدوجة: السجل (`alreadyOut`) + `notifiedTodayFor` بتوقيت `Asia/Riyadh`.
- **حذف التنبيه يسحب نسخه** من كل الأجراس (`deleteNotificationsForEntity`، عبر `entityId`).
- **شاشة الإرسال تقول الحقيقة:** شريط تحذير دائم بعدد من لم يفعّل حسابه (`parentActivation`)، ورسالة بعد الإرسال تفصّل من وصله فعلاً.
- **الجرس يحدّث نفسه كل ٦٠ ثانية** أثناء ظهور التبويب فقط (`document.visibilityState`).
- جرس في صفحة إعدادات المعلم — كانت الوحيدة الناقصة.

**ما لم يُتحقق منه:** لم أرَ جرساً واحداً بعينيّ — لا أملك كلمة مرور لأي حساب. المتحقَّق: `tsc` نظيف، البناء ناجح، جملة منع التكرار جُرِّبت على قاعدة البيانات الحيّة، وقاعدة البيانات تثبت أن مسار الكتابة يعمل.

### (١٦) هذا الملف

> **المستخدم:** حسنا قم بعمل ملف lm بهذه المحادثه من اول ملف وحرف بها وكل شي حتي اعطيه لك في محادثه جديده ويعرف ما الذي تم وتحدثنا بيننا فيه وكل شي بالحرف لا تنسي اي شي من الاول حتي هذه اللحظه

---

## ٤. الملفات المحورية

### مكتبات مشتركة

| الملف | الدور |
|---|---|
| `lib/utils.ts` | `wholePercents` (تقريب largest-remainder)، `isValidDateString`، `parentPhoneKey`، `parentEmailCandidates`، `today`/`schoolDate` بتوقيت `Asia/Riyadh`، `formatDateAr` |
| `lib/teacher-access.ts` | `requireTeacherForClass` / `getTeacherClassAccess` / `getTeacherVisibleClassIds` — تقييد بالمادة مع تراجع آمن: الفصل الذي لم تُسنَد له مادة بعد يبقى مرئياً للجميع |
| `lib/points.ts` | `attendancePointsFor` / `lessonPointsFor` / `resyncSchoolPoints` (يعيد حساب الجدولين) |
| `lib/daily-roster.ts` | `getRosterForDay` — **نُقلت من ملف `'use server'` لأن التصدير منه جعلها endpoint عاماً** |
| `lib/behavior-cases.ts` | `listCases` / `countCasesBy*` — **لا تختار `counselorNote` أبداً** |
| `lib/case-status.ts` | تسميات آمنة للعميل فقط (لا تستورد `db`) — أُنشئ لأن استيراد `CASE_STATUS` من `behavior-cases` في مكوّن عميل كسر البناء بـ `Can't resolve 'dns'/'fs'` |
| `lib/counselor-access.ts` | `getCounselorAccess` / `requireCounselor` / `getCounselorClassIds` / `requireCounselorForStudent` |
| `lib/notifications.ts` | كل شيء عن الإشعارات — انظر أدناه |

### `lib/notifications.ts` — الواجهة الحالية

```
NOTIFICATION_KINDS: announcement | case_raised | case_decided |
                    case_escalated | case_returned | parent_informed | absence
notify(entries)                       // يبتلع أخطاءه — لا يُسقِط الحدث الذي سبّبه
counselorsForClass(schoolId, classId)
parentOfStudent(studentId)
parentsForAnnouncement(schoolId, {studentId, classId})
staffForAnnouncement(schoolId, exceptUserId) → [{userId, href}]
parentReach(schoolId, recipientIds)   → {total, reachable}
parentActivation(schoolId)            → {total, activated}
notifiedTodayFor(schoolId, kind, entityIds) → Set<entityId>
deleteNotificationsForEntity(schoolId, entityId)
listInbox / countUnread / markRead / markAllRead   // كلها مقيّدة بـ userId القارئ
```

### الجداول المضافة في هذه المحادثة

- `daily_records`: عمودا `absence_marked_by` (text)، `absence_marked_at` (timestamptz)
- `lesson_records` (جديد): فريد على (طالب، معلم، يوم)
- `behavior_cases` (جديد، ١٩ عموداً): `... status, owner_user_id, counselor_note, admin_note, decided_by_user_id, decided_at, escalated_to_user_id, escalated_at, parent_message_sent ...`
- `user_notifications` (جديد، ١١ عموداً): `school_id, recipient_user_id, kind, title, body, href, entity_id, actor_name, read_at, created_at`
- `school_staff.role` أُضيف إليه `'counselor'`

### أوامر الخادم

| الملف | أبرز الدوال |
|---|---|
| `app/teacher/(dashboard)/actions.ts` | `saveDailyRecords` (قفل الغياب + إشعار الغياب)، `raiseBehaviorCase`، `getMyCaseStatuses`، `getAbsenceLocks` |
| `app/counselor/(dashboard)/actions.ts` | `getEscalationTargets` (تصفية بالمرحلة **واستبعاد الوكيل للقراءة فقط**)، `resolveCasePrivately`، `dismissCase`، `informParent` (تعيد `waUrl`)، `escalateCase`، `getCaseContext`، `getParentContact` |
| `app/admin/(dashboard)/cases/actions-cases.ts` | `adminHandleCase`، `adminInformParent`، `returnCaseToCounselor`، `getCaseParentContact` |
| `app/admin/(dashboard)/notifications/actions-notifications.ts` | `sendNotification` (بوجهة)، `deleteNotification` (يسحب النسخ) |
| `app/notifications-actions.ts` | `getMyNotificationsInbox` / `markNotificationsRead` / `markAllNotificationsRead` — كلها على صندوق صاحب الجلسة فقط، **لا معرّف يُمرَّر يمكن أن يسمّي غيره** |

### مكوّنات

- `components/notification-bell.tsx` — جرس واحد لكل البوابات، يعرف وجهته من `usePathname()`، تحديث كل ٦٠ث أثناء الظهور فقط.
- `components/notifications-page.tsx` — صندوق الوارد الكامل، تستعمله البوابات الأربع، ويحوي الجرس في رأسه.
- `components/portal-layout.tsx` — **لا يحوي جرساً ولا شريطاً علوياً** (أُلغي بطلب المستخدم).

---

## ٥. الأخطاء التي وقعت — بما فيها أخطائي

| # | الخطأ | السبب | الحال |
|---|---|---|---|
| ١ | النِسب لا تجمع ١٠٠ | متأخر + بعذر غير مرئيين، وتقريب عادي | أُصلح — largest-remainder |
| ٢ | رسم الاتجاه يرسم أيام «لا بيانات» صفراً | `hasAttendance` موجود لكن `TrendChart` لم يستعمله | أُصلح — gap segments |
| ٣ | **خطئي:** ضياع ٦ نقاط لكل طالب | الهجرة نسخت بـ `points_earned = 0` وافترضت تغطية المجموع القديم | أُصلح — إعادة حساب الجدولين |
| ٤ | **خطئي:** لوحة الصدارة ١ بدل ١٢ | عدّلت بيانات الإنتاج والموقع يعمل بكود قديم، وقلت «لا يحتاج رفع كود» | أُصلح — والقاعدة مسجّلة |
| ٥ | **خطئي الأمني:** `getRosterForDay` بلا حراسة | صُدِّرت من ملف `'use server'` | نُقلت إلى `lib/daily-roster.ts` |
| ٦ | **خطئي:** `app/api/devcheck` بقي في الشجرة | فشل `curl` قطع سلسلة `&&` قبل `rm` | حُذف، وأُضيف لـ `.gitignore` (لم يُرفع قط) |
| ٧ | **خطئي:** عدّاد نجاح الاسترجاع منتفخ | عدّادان تشخيصيان دخلا المجموع | أُصلح — الجمع على مفاتيح `RESTORE_TABLE_LABELS` فقط |
| ٨ | **عطل إنتاج:** أولياء الأمور لا يدخلون | البريد يُبنى من الأرقام كما تُكتب، والمخزّن بصيغة أخرى | أُصلح — `parentEmailCandidates` |
| ٩ | واتساب لا يفتح عند التأكيد | `window.open` بعد `await` محجوب | أُصلح — فتح متزامن داخل النقرة |
| ١٠ | فشل بناء `Can't resolve 'dns'/'fs'` | مكوّن عميل استورد من ملف يستورد `db` | أُصلح — `lib/case-status.ts` |
| ١١ | قائمة الجرس مقصوصة في الشريط الجانبي (٢٥٦px) وغير مرئية على الجوال | الجرس داخل القائمة الجانبية | أُصلح — نُقل لرأس الصفحات |
| ١٢ | **آخر جولة:** لا إشعار يصل لأحد | ثلاثة أسباب — انظر القسم ٣ (١٥) | أُصلح ما يُصلَح بالكود؛ يبقى جدار التفعيل |

---

## ٦. أشياء بدت أخطاءً وليست كذلك (تم التحقق — لا تعِد فتحها)

المستخدم طلب صراحةً ألا نتسرّع بالحكم. هذه فُحصت وثبتت سلامتها:

- **تصفية الفصل في لوحة الصدارة** — `getLeaderboard` تُرجع كل الطلاب بلا حد.
- **استبعاد النقاط الصفرية والسالبة من لوحة الشرف** — مقصود.
- **عدم تغيير بريد دخول المعلم حين يتغيّر هاتفه** — مقصود، وموثّق بتعليق في الكود.
- **مدير الجودة يدير مديري جودة آخرين** — مطابق لتصميم الأدوار الموثّق.
- **أعمدة `temp_password`** — استُعلمت قاعدة البيانات الحيّة: **صفر صف** يحوي كلمة مرور نصية.
- **أقفال غياب يتيمة** — **صفر**.
- **تصدير Excel** — موجود أصلاً في `grade-levels`.
- **عائلات مقسّمة بسبب صيغة الهاتف** — لا توجد؛ السجل الوحيد بصيغة `05…` كان «طالب تجريبي».
- **`navConfig.admin` في `portal-layout.tsx`** — يبدو ناقصاً لرابط «إشعاراتي»، لكنه **كود ميت**: `app/admin/(dashboard)/layout.tsx` يمرّر `links` فيتجاوزه، وهي تحوي `/admin/my-notifications`.
- **الصفحات التي «ينقصها» جرس** — `notifications/page.tsx` الأربع (الجرس داخل المكوّن المشترك)، و`classes/[classId]/page.tsx` (الجرس في `class-roster.tsx`)، و`app/page.tsx` (شاشة اختيار بوابة، بلا جلسة).

---

## ٧. حقائق قاعدة البيانات الحيّة (٢٠٢٦-٠٩-٠٩ ~٠٥:٢٠)

```
الجداول: account, attendance, audit_log, behavior_cases, classes, daily_records,
        grade_entries, grade_levels, lesson_records, notifications,
        parent_whatsapp_messages, school_staff, schools, session, student_points,
        students, subjects, teachers, user, user_notifications, verification

user_notifications : 281 صفاً — كلها kind='announcement'، صفر مقروء
behavior_cases     : 6 صفوف — 2 resolved_privately، 3 parent_informed، 1 admin_handled
lesson_records     : 444 صفاً
حسابات أولياء الأمور: 281 — منها 2 فقط مفعّلة (غيّرت كلمة المرور)
حسابات الطاقم/المعلمين: 24 — كلها مفعّلة
```

**فريق الإدارة:**

| الدور | الاسم | النطاق |
|---|---|---|
| `counselor` | ا \ عمر | الابتدائية + المتوسطة |
| `deputy` | أ / أحمد حلمي أحمد | الابتدائية + المتوسطة |
| `principal` | د \ محمد صديق | الابتدائية + المتوسطة |
| `quality_manager` | د \ خالد ابو الهنا | كل المراحل |

**المراحل:** «المرحلة الابتدائية ( بنين )» ٧ فصول، «المرحلة المتوسطة ( بنين )» ٧ فصول — **وكلتاهما مغطاة بالموجه**، فمسار `counselorsForClass` سيعمل.

---

## ٨. ما لم يُبنَ بعد (المستخدم يعلم)

- تقارير أسبوعية/شهرية للمعلم.
- إشعارات من المعلم إلى ولي الأمر يبدأها المعلم بنفسه.
- إجبار المعلمين الـ١٩ والطاقم الـ٣ الحاليين على تغيير كلمات مرورهم — الكود يطبّقه على الحسابات الجديدة وإعادة التعيين فقط. **ينتظر موافقة المستخدم.**
- تخزين حدّ المعدّل (rate limit) في قاعدة البيانات — أُجِّل عمداً: خطأ في استنتاج الـIP قد يقفل المدرسة كلها، ولا يمكن اختباره من هنا. الإعداد الحالي: `ipAddressHeaders: ['x-vercel-forwarded-for','x-real-ip','x-forwarded-for']`، ٦٠ محاولة دخول/دقيقة (المدرسة تشترك في IP واحد).
- إسناد المواد لـ ٨ من ١٤ فصلاً — **مهمة بيانات على المستخدم نفسه**، وحتى تكتمل يبقى الفصل غير المُسنَد مرئياً لكل المعلمين (تراجع آمن مقصود).

---

## ٩. القرار المعلّق الوحيد — ينتظر المستخدم

**٢٧٩ ولي أمر لن يصلهم أي إشعار** حتى يدخلوا ويختاروا كلمة مرور. لا إشعار غياب، ولا تنبيه، ولا رسالة موجّه. هذا ليس عطلاً — هو الحماية التي وُضعت عمداً كي لا يفتح أحد حساب ابن غيره بكلمة المرور المشتركة.

عُرض عليه خياران ولم يجب بعد:
1. **حملة واتساب** تطلب من الأولياء الدخول مرة واحدة وتفعيل الحساب. *(الأفضل)*
2. **إلغاء الإجبار** وقبول بقاء كلمة المرور المشتركة — **لا أنصح به**؛ بيانات الأبناء تصير مكشوفة لكل من يعرف الصيغة.

---

## ١٠. قائمة الـ commits في هذه المحادثة

```
70e56d5  2026-09-09 05:21  تشغيل نظام الإشعارات فعلياً: تنبيه الطاقم، إشعار الغياب، وسحب المحذوف
5028448  2026-09-09 01:56  إلغاء شريط الجرس ووضعه في رأس كل صفحة
a189c52  2026-09-08 22:33  نقل جرس الإشعارات إلى شريط علوي في الصفحة
6a23a3c  2026-09-08 22:20  نظام إشعارات موحّد وجرس في البوابات الأربع
fbfe308  2026-09-08 21:57  فتح واتساب عند تأكيد إبلاغ ولي الأمر
677e42d  2026-09-08 21:48  الوكيل يتصرّف في الحالة المحالة، والموجه يرى ما جُرِّب من قبل
cf9bf4b  2026-09-08 21:18  الموجه الطلابي: لا تصل ملاحظة سلبية إلى بيت قبل أن يقرأها
ec59961  2026-09-08 18:55  مراجعة بوابة المدير قسماً قسماً: إصلاح سبع مشكلات
54a6d02  2026-09-08 18:11  إصلاح تعذّر دخول أولياء الأمور بصيغة الرقم المعتادة
c91c1a3  2026-09-08 17:42  إغلاق تسريب سجل الفصل وإجبار المعلم على تغيير كلمة مروره
02de029  2026-09-08 17:10  سد خمس ثغرات صلاحيات في بوابتَي الإدارة وولي الأمر
91933fe  2026-09-08 16:13  إصلاح استرجاع النسخ الاحتياطية وسبع مشكلات في شاشة المعلم
484d51d  2026-09-08 12:48  فصل الحضور عن تقييم كل معلم
```

---

## ١١. قواعد مستخلَصة — التزم بها

1. **كل `export` من ملف `'use server'` هو نقطة نهاية HTTP عامة.** لا تصدّر منه إلا ما تحرسه بنفسه. الحراسة على الصفحة لا تحمي الدالة.
2. **Next.js يحذف نص الاستثناء في بناء الإنتاج.** أعِد `{ ok: false, error }` بدل `throw`، وإلا رأى المستخدم رسالة عامة لا معنى لها.
3. **انشر الكود قبل أن تهاجر البيانات**، وتحقق من `git log` قبل أن تدّعي أن الكود منشور.
4. **`window.open` بعد `await` محجوب.** افتح التبويب داخل إيماءة النقر.
5. **حزمة العميل يجب ألا تستورد ما يجرّ `pg`** — فشل حلّ `dns`/`fs`.
6. **احذف الملفات المؤقتة بـ `;` لا `&&`** — فشل الأمر السابق يمنع الحذف.
7. **ملفات المشروع CRLF**؛ الاستبدال بـ Python قد يفشل بصمت — استخدم `Edit` عند الشك.
8. **العربية في الطرفية تظهر مشوّهة** — خلل عرض لا خلل بيانات.
9. **`formatDateAr` يستخدم `'ar-SA'`** بينما الجرس يستخدم `'ar-SA-u-ca-gregory'` — الأول قد يعرض التقويم الهجري. لم يُغيَّر لأنه مستخدم في كل تواريخ النظام والاتساق أولى؛ إن طُلب تغييره فغيّره في مكان واحد.
10. **قل ما لم تتحقق منه.** المستخدم يقبل «لم أتحقق»، ولا يقبل ادّعاءً يتبيّن خطؤه.

---

*آخر تحديث: ٢٠٢٦-٠٩-٠٩ ٠٥:٢٥ — نص رسائل المستخدم الكامل محفوظ أيضاً في مخرجات الجلسة، والسجل الخام في `C:\Users\CENTER_ELRahama\.claude\projects\F--aws\6e30ed68-fe7f-4726-8d4f-2fa1eef5c3a3.jsonl`.*
