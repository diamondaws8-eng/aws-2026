import { NextResponse } from 'next/server'; import { addStudent } from '@/app/admin/(dashboard)/students/actions-students'; export async function GET() { try { const res = await addStudent({ fullName: 'test', nationalId: '123', parentPhone: '050000000', gender: 'male', classId: '', schoolId: 'fake' }); return NextResponse.json(res); } catch (e: any) { return NextResponse.json({ error: e.message || String(e), stack: e.stack }); } }
